# Changelog

All notable changes to `SYSTEM — Update Task Protocol (UT S)` are documented here.

## 1.4.1 - 2026-07-26
- Fixed `homepage` field: was pointing at the `openaiskillpackage.com` spec site instead of `penrithbeacon.com`.

## 1.4.0 - 2026-07-26
- Initial standalone publication, split out of Cup and Ring Task Master's built-in
  `system-skills.js` for bundling into the `.aistarter` system-starter set.
